apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "smartorganizr.fullname" . }}-test-connection"
  labels:
    {{- include "smartorganizr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "smartorganizr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
