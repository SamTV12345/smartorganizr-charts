apiVersion: v1
kind: Service
metadata:
  name: {{ include "smartorganizr.fullname" . }}
  labels:
    {{- include "smartorganizr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smartorganizr.selectorLabels" . | nindent 4 }}
